package Coding;

public class Sample
{
//    2. Write a class with following fields and functions
//	* Map<String, Data>
//	* init() which initialises this map
//	* reload, which will be called by some listener to re-init this map with new values
//   * getData, which takes String name


    //HashMap<String, Data> dataMap = new HashMap();
    //reload()
    // int() --> Readwrite.lock(dataMap);
    //inialises the map
    //unlock(datamap);
    //getData(String name)
    //ReadWirte.readlock(dataMap)
    //dataMap.containsKey(name);
    //if Present -> return Data -> dataMap.get(name)
    // if not present -> return null
    // unlock(dataMap;


}
